<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Inmobiliaria - Inicio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Bootstrap y Font Awesome -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: #f4f6f9;
            color: #2c3e50;
            margin: 0;
        }

        header {
            background: #1e272e;
            padding: 1rem 0;
        }

            header h2 {
                color: #fff;
                margin: 0;
            }

        .navbar .nav-link {
            color: #f1f1f1 !important;
            margin-left: 1rem;
            font-weight: 500;
            transition: 0.3s;
        }

            .navbar .nav-link:hover {
                color: #ffc107 !important;
            }

        .main-banner {
            background: linear-gradient(to right, rgba(0,0,0,0.6), rgba(0,0,0,0.3)), url('https://images.unsplash.com/photo-1600585154340-be6161a56a0c?fit=crop&w=1920&q=80') center/cover no-repeat;
            color: white;
            padding: 6rem 2rem;
            border-radius: 1rem;
            text-align: center;
            margin: 2rem auto;
            max-width: 1100px;
        }

        .btn-banner {
            background-color: #ffc107;
            color: #000;
            padding: 0.8rem 2rem;
            font-weight: 600;
            border: none;
            border-radius: 50px;
            margin-top: 2rem;
            transition: background 0.3s;
        }

            .btn-banner:hover {
                background-color: #e0a800;
            }

        .card {
            border: none;
            border-radius: 1rem;
            transition: 0.3s ease-in-out;
        }

            .card:hover {
                transform: translateY(-6px);
                box-shadow: 0 10px 25px rgba(0,0,0,0.08);
            }

        footer {
            background: #1e272e;
            color: #ccc;
            text-align: center;
            padding: 1rem;
            margin-top: 3rem;
            font-size: 0.9rem;
        }

        /* Modal Login */
        .login-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.45);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }

        .login-card {
            background: white;
            border-radius: 1.2rem;
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            position: relative;
        }

            .login-card h3 {
                margin-bottom: 1rem;
                font-weight: 700;
            }

            .login-card .form-control {
                border-radius: 0.75rem;
                margin-bottom: 1rem;
            }

        .btn-login {
            background: #007bff;
            border: none;
            color: white;
            padding: 0.75rem;
            width: 100%;
            font-weight: 600;
            border-radius: 50px;
        }

            .btn-login:hover {
                background: #0056b3;
            }

        .close-btn {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 1.3rem;
            color: #444;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header>
        <div class="container d-flex justify-content-between align-items-center">
            <h2><i class="fas fa-building me-2"></i>Inmobiliaria</h2>
            <nav class="navbar navbar-expand-md">
                <ul class="navbar-nav flex-row">
                    <li class="nav-item"><a href="#" class="nav-link">Inicio</a></li>
                    <li class="nav-item"><a href="#" class="nav-link" id="abrirLogin">Iniciar Sesión</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Banner -->
    <div class="container">
        <section class="main-banner">
            <h1 class="display-5 fw-bold">Tu próximo hogar te espera</h1>
            <p class="lead">Explora propiedades, arriendos y nuevos proyectos en todo el país.</p>
            <button class="btn btn-banner" id="mostrarLogin">Ingresar al sistema</button>
        </section>

        <!-- Servicios -->
        <div class="row text-center mt-5">
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm p-3">
                    <div class="card-body">
                        <i class="fas fa-city fa-2x mb-2 text-primary"></i>
                        <h5 class="card-title">Proyectos Nuevos</h5>
                        <p>Explora construcciones modernas en zonas exclusivas.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm p-3">
                    <div class="card-body">
                        <i class="fas fa-home fa-2x mb-2 text-success"></i>
                        <h5 class="card-title">Propiedades en Venta</h5>
                        <p>Encuentra tu hogar ideal entre cientos de opciones disponibles.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm p-3">
                    <div class="card-body">
                        <i class="fas fa-calendar-check fa-2x mb-2 text-warning"></i>
                        <h5 class="card-title">Visitas y Arriendos</h5>
                        <p>Programa visitas y revisa los arriendos activos fácilmente.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 Inmobiliaria. Todos los derechos reservados.</p>
    </footer>

    <!-- Login Modal -->
    <div class="login-modal" id="loginModal">
        <div class="login-card">
            <span class="close-btn" id="cerrarModal">&times;</span>
            <h3 class="text-center"><i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión</h3>
            <form id="loginForm">
                <input type="email" class="form-control" id="username" placeholder="Correo electrónico" required>
                <input type="password" class="form-control" id="password" placeholder="Contraseña" required>
                <button type="submit" class="btn-login">Ingresar</button>
                <div id="mensaje" class="text-danger mt-2 text-center"></div>
            </form>
        </div>
    </div>

    <!-- Script -->
    <script>
        const API_URL = 'https://localhost:44375/api/Login/Ingresar';

        document.getElementById("mostrarLogin").onclick =
            document.getElementById("abrirLogin").onclick = () => {
                document.getElementById("loginModal").style.display = "flex";
            };

        document.getElementById("cerrarModal").onclick = () => {
            document.getElementById("loginModal").style.display = "none";
        };

        document.getElementById('loginForm').addEventListener('submit', async function (e) {
            e.preventDefault();
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            const mensaje = document.getElementById('mensaje');

            try {
                const res = await fetch(API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ Usuario: username, Clave: password })
                });

                if (res.ok) {
                    const data = await res.json();
                    localStorage.setItem('token', data.Token);
                    localStorage.setItem('usuario', data.Usuario);
                    window.location.href = "/Public/dashboard.html";
                } else {
                    mensaje.textContent = "Credenciales incorrectas. Intenta nuevamente.";
                }
            } catch {
                mensaje.textContent = "Error de conexión con el servidor.";
            }
        });
    </script>
</body>
</html>
